export const TICK_RATE_MS = 150;
export const FLEE_RADIUS = 8;
export const GRID_COLS = 40;
export const GRID_ROWS = 30;
export const SCATTER_DELAY_MS = 1500;

export const GAME_CONSTANTS = {
  TICK_RATE_MS,
  FLEE_RADIUS,
  GRID_COLS,
  GRID_ROWS,
  SCATTER_DELAY_MS,
};

export default GAME_CONSTANTS;
